defmodule GameNight.SchedulesFixtures do
  @moduledoc """
  Reusable factory helpers for the `Schedules` domain in tests.

  Skeleton — concrete helpers (`build_schedule_attrs/1`,
  `system_create_preparing_schedule/2`, `add_player_link/3`,
  `system_transition_to_ready/1`, `system_post_schedule/2`) land in
  T019 (Foundational). Tests alias this module explicitly per the
  project convention (no auto-import via `data_case.ex`).
  """
end
