[
  # Each entry MUST include a code comment explaining why Dialyzer is
  # being asked to ignore the warning. Format:
  #
  #   {"path/to/file.ex", :warning_type, line_number}
  #   # Why: brief justification
]
